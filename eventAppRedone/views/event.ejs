<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= event.title %> - The Divine Crustacean</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles/index.css">
</head>
<body>
    <div class="wrapper">
        <%- include('partials/header') %>
        <main class="content container-fluid p-0">
            <div class="container">
                <div class="row">
                    <div class="col-md-4">
                        <img src="<%= event.image %>" class="img-fluid rounded" alt="<%= event.title %>">
                    </div>
                    <div class="col-md-8">
                        <h2 class="mt-3"><%= event.title %></h2>
                        <ul class="list-group list-group-flush">
                            <li class="list-group-item"><strong>Category:</strong> <%= event.category %></li>
                            <li class="list-group-item"><strong>Details:</strong> <%= event.details %></li>
                            <li class="list-group-item"><strong>Start Date and Time:</strong> <%= event.startDateTime %></li>
                            <li class="list-group-item"><strong>End Date and Time:</strong> <%= event.endDateTime %></li>
                            <li class="list-group-item"><strong>Location:</strong> <%= event.location %></li>
                            <li class="list-group-item"><strong>Host:</strong> <%= event.host %></li>
                        </ul>
                        <% if (user && event.user.equals(user._id)) { %>
                            <a href="/events/edit/<%= event._id %>" class="btn btn-warning mt-3">Edit</a>
                            <form action="/events/delete/<%= event._id %>" method="POST" style="display:inline;">
                                <button type="submit" class="btn btn-danger mt-3">Delete</button>
                            </form>
                        <% } %>
                    </div>
                </div>
            </div>
        </main>
        <%- include('partials/footer') %>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <script src="/scripts.js"></script>
</body>
</html>
